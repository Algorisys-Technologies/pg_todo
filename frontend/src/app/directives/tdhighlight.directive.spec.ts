import { TdhighlightDirective } from './tdhighlight.directive';

describe('TdhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TdhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
